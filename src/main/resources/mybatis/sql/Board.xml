<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hacs.mvc.repository.BoardRepository">


	<select id="getList" parameterType="hacs.mvc.domain" resultType ="hacs.mvc.domain.Board">
		select
			B.BOARD_SEQ,
			B.TITLE,
			B.CONTENTS,
			B.REG_DATE
		from T_BOARD B
		ORDER BY B.REG_DATE DESC
		<!-- <where>
			<if test ="@org.apache.commons.lang3.StringUtils@isNotEmpty(keyword)">
				AND B.TITLE LIKE CONCAT('%' , ${keyword} , '%')
			</if>
		</where> -->
	</select>
	
	<select id="get" parameterType="int" resultType ="hacs.mvc.domain.Board">
		select
			B.BOARD_SEQ,
			B.TITLE,
			B.CONTENTS,
			B.REG_DATE
		from T_BOARD B
		WHERE B.BOARD_SEQ = #{boardSeq}
	</select>
	
	
	<insert id="save" parameterType="hacs.mvc.domain.Board" useGeneratedKeys = "true" keyProperty = "boardSeq">
		INSERT INTO T_BOARD
		(
			TITLE,
			CONTENTS,
			REG_DATE
		)
		VALUES
		(
			#{title},
			#{contents},
			NOW()
		)
	</insert>
	
	<insert id="saveList" parameterType="map" >
		INSERT INTO T_BOARD
		(
			TITLE,
			CONTENTS,
			REG_DATE
		)
		VALUES
		<!-- collection은 map에 담겨있는 key값을 말함 -->
		<foreach collection="boardList" item="board" separator=",">
			(
				#{board.title},
				#{board.contents},
				NOW()
			)
		</foreach>
		
	</insert>
	
	
	<update id="update" parameterType="hacs.mvc.domain.Board" >
		UPDATE T_BOARD
		SET
			TITLE = #{title},
			CONTENTS = #{contents}
		WHERE BOARD_SEQ = #{boardSeq}
	</update>
	
	<delete id="delete" parameterType="int" >
		DELETE 
		FROM
			T_BOARD
		WHERE BOARD_SEQ = #{boardSeq}
	</delete>
	
</mapper>